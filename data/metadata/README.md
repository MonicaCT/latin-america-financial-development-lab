# Metadata

Data inventories, load status tables, and validation logs are generated here.
The project writes metadata even when raw data are missing so that replication
failures are transparent rather than silent.
